# 后续发展规划 & 已知 Bug

## 短期目标（v0.2.0）
- 确保管家agent所有功能可用，增加一些特殊工具,功能案例等
    - 给个API填写工具（ask交互工具升级）
    - 一个知识库，汇总碰到的所有问题
    - 允许用户手动清理加载的文件，避免AI忘记清理，以及当文件到达指定的数量时自动触发清理逻辑。

## 中期目标（v0.3.0）
- 统一使用vh,vw,百分比等方式来画前端大小，这样确保大中小设备都一样，甚至在手机上都是一样跑（现在可以试着在手机上部署玩玩）
- subagent,多agent系统
- 拖拽功能需要跨标签页
- 知识库页面的结果组件，点击其他地方应该自行关闭
- 长期记忆功能
- 搜索工具的返回结果，可能需要添加id（段落-两位哈希）
- 删除文件后，需要更新ai的加载文件列表
- git存档功能支持分支
- 部分功能自动化测试
- 多模态，上传图片
- 可视化工作流编辑器（类dify）
- 消息回档的左右翻页
- 解除中断时，用户消息，工具结果应该乐观更新，而且按照顺序，*先显示工具结果，再显示用户消息*，可能需要重构整个后端，将不同的存档点数据合并，然后再发给前端渲染。这样可以同时解决消息翻页功能
- comfyUI相关功能
- 更灵活的AI聊天功能（类酒馆？？？）
- 工具加强
- 中断时通知提供商停止生成？这个需要参考别人的项目看看怎么实现的
- 允许用户打开文件夹作为工作目录
- 新增memory_edit工具，允许ai直接编辑自己的上下文（？暂时还没有思路）

## 长期目标（v1.0.0）
- 暂无

## 已知 Bug

1. 当 AI 先前使用过工具（如 `write_file`），后续关闭该工具的使用权限与使用说明。一旦 AI 试图调用这个关闭的工具，就会报错。但是可控，渲染崩溃后，打开消息，删掉最后一个调用就好。

2. 重命名文件夹时，中间的标签页，标签没有同步更新

3. 嵌入内容时，切换到其他页面，可能导致进度停滞卡住，但实际已经嵌入完毕

4. deepseek-v4带来的问题，我觉得使用a社兼容接口可能解决问题