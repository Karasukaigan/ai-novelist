LangChain文档，SSE（Server-Sent Events）在MCP规范中已被标记为废弃。文档在“Passing headers”一节中明确提到：

    This is supported for sse (deprecated by MCP spec) and streamable_http transports.

也就是说，虽然当前版本的 langchain-mcp-adapter 仍然支持 SSE，但 MCP 官方规范已经不再推荐使用它，取而代之的是 streamable_http（在文档示例中直接称为 http 传输）